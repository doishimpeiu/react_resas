import React from 'react'

const Pref = ({todos}) => {
  // todos.map((i) => {
  //   console.log(i.prefName);
  // });

  return (
    <div>
    {/* {todos.map((i) => (
    <label>
      <input type="checkbox" />{i.prefName}
    </label>
    ))} */}
  </div>
  )
}

export default Pref
